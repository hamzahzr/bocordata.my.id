import './globals.css';

export const metadata = { title: 'BocorData.my.id', description: 'Secure data exposure checker' };

export default function RootLayout({ children }) {
  return <html lang="id"><body>{children}</body></html>;
}
